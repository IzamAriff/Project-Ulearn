package com.ulearn.main;

import java.sql.SQLException;

public class Main {

    public static void main(String[] args) throws SQLException {

        /*
        // create an instance of the StudentDataAccess class
        StudentDataAccess studentData = new StudentDataAccess();

        // create a new student record in the database
        Student newStudent = new Student("John Doe", "12345", 21, "IC123456", "Computer and Information Science", "john.doe@example.com", "Computer Science");
        StudentDataAccess.addStudent(newStudent);

        // retrieve a student record from the database
        Student retrievedStudent = StudentDataAccess.getStudentById("12345");
        System.out.println("Retrieved student: " + retrievedStudent);

        // update a student record in the database
        retrievedStudent.setAge(22);
        StudentDataAccess.updateStudent(retrievedStudent);

        // delete a student record from the database
        StudentDataAccess.deleteStudent("12345");

         */

    }
}
